# Step 1 : We created 2 folders under the AI_ML_PROJECT
# FRONTEND - All code for front end
# BACKEND - All code for backend

# Step 2 : Activatate a virtual environment

# to chek python version
python --version

# win
python -m venv ven_name(change this as per usage)

# mac
python -m venv ven_name

# activation
source ven_name/bin/activate
venv\Scripts\activate
venv\Scripts\activate .\aiml_001\ 

# in this case it would be
aiml_001\Scripts\activate .\aiml_001\
# deactivation
deactivate

# to install the required libraries
 pip install -r requirements.txt

